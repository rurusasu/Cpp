#include <bits/stdc++.h>

using namespace std;

int main()
{
  double b = 0.0;
  double current_rait = 0.0, next_rait = 0.0;

  cin >> current_rait >> next_rait;
  b = next_rait * 2 - current_rait;
  cout << b << endl;
}